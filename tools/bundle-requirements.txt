# Dependencies embedded into each supported CPython ABI vendor directory.
# Add future binary or pure-Python runtime dependencies here; build_release.py
# resolves and bundles their wheel dependencies for every supported ABI.
Pygments==2.20.0
RapidFuzz==3.14.5
wcwidth==0.2.13
